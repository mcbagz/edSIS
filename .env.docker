# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=sis_secure_password_change_this
POSTGRES_DB=sis_db

# Application Configuration
NODE_ENV=production
JWT_SECRET=your_very_secure_jwt_secret_change_this_in_production

# Frontend Configuration
VITE_API_URL=http://localhost:5000/api
FRONTEND_URL=http://localhost

# AWS Configuration (Optional - for document uploads and emails)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET_NAME=sis-application-documents
AWS_SES_FROM_EMAIL=noreply@yourschool.edu

# Ed-Fi Integration
EDFI_API_BASE_URL=http://localhost/api
EDFI_API_CLIENT_ID=your_edfi_client_id
EDFI_API_CLIENT_SECRET=your_edfi_client_secret

# Ed-Fi Sync Service
SYNC_INTERVAL=300000  # 5 minutes in milliseconds

# PgAdmin (Development only)
PGADMIN_EMAIL=admin@school.edu
PGADMIN_PASSWORD=admin123